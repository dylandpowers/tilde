### This file was generated by Nexus Schema
### Do not make changes to this file directly


"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type JournalEntry {
  categories: [String!]!
  date: DateTime!
  id: ID!
  isArchived: Boolean!
  text: String!
}

type Mutation {
  addBook(book: String!): String!
  addJournalEntry(categories: [String!]!, text: String!): JournalEntry
  addReadingActivity(book: String!, minutes: Int!): ReadingActivity!
  addTodo(text: String!): Todo!
  archiveJournalEntry(id: ID!): ID
  completeTodo(id: ID!): Todo!
}

type Query {
  activities(month: Int): [ReadingActivity]
  books: [String]
  completedTodos: [Todo]!
  entries: [JournalEntry]!
  hooray: String!
  todos(ids: [String]): [Todo]
  token: String!
  weather: Weather!
}

type ReadingActivity {
  book: String!
  date: DateTime!
  id: ID!
  minutes: Int!
}

type Todo {
  createdAt: DateTime
  id: ID!
  isCompleted: Boolean!
  text: String!
  updatedAt: DateTime
}

type Weather {
  degrees: String!
  iconUrl: String!
}
